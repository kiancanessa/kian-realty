// Single source of truth for contact numbers that appear in more than one
// place, so the floating button and the team card can never drift apart.
// Digits only, international format — this is what wa.me expects.
export const JORGE_WHATSAPP = "5216611256107";
